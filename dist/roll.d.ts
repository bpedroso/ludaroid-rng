import { SeededRNG } from './SeededRNG.js';
/** Returns true when rng.next() < threshold (threshold in [0, 1]). */
export declare function roll(rng: SeededRNG, threshold: number): boolean;
//# sourceMappingURL=roll.d.ts.map